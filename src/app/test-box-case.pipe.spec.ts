import { TestBoxCasePipe } from './test-box-case.pipe';

describe('TestBoxCasePipe', () => {
  it('create an instance', () => {
    const pipe = new TestBoxCasePipe();
    expect(pipe).toBeTruthy();
  });
});
